---
trigger: always_on
---

- Read `AGENTS.md` before making structural or behavioral changes.
- Use `pnpm`; do not use `npm`.
- Use `pnpm build` as the baseline verification command.
- Keep AI-agent documentation in `AGENTS.md` or `docs/TECHNICAL_REFERENCE.md`.
- Do not add new root-level generated research Markdown files.
